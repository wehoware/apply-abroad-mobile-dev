const AppConstants = {
  animationTiming: 500,
  passwordError:
    "Password must be of minimum 8 characters long, should contain one capital letter, one special character, one numerical number",
};

export default AppConstants;
